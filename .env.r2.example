# ================================
# Cloudflare R2 存储配置
# ================================
# 注册账号: https://dash.cloudflare.com/
# 创建 R2 存储桶: https://dash.cloudflare.com/?to=/:account/r2

# 1. 创建 R2 Bucket（存储桶）
#    - 访问 Cloudflare Dashboard → R2 → Create bucket
#    - 输入 bucket 名称，例如: nwpushare-assets
#    - 记录下 Bucket 名称

# 2. 创建 API Token（访问密钥）
#    - 在 R2 页面点击 "Manage R2 API Tokens"
#    - 点击 "Create API Token"
#    - 权限选择 "Object Read & Write"
#    - 记录下 Access Key ID 和 Secret Access Key

# 3. 获取 Account ID
#    - 在 Cloudflare Dashboard 右侧可以看到你的 Account ID
#    - 或者在 R2 页面的 URL 中也能找到

# 4. 配置环境变量（填入下方）

# R2 Access Key ID（类似 AWS Access Key）
KUN_VISUAL_NOVEL_S3_STORAGE_ACCESS_KEY_ID=your_r2_access_key_id

# R2 Secret Access Key（类似 AWS Secret Key）
KUN_VISUAL_NOVEL_S3_STORAGE_SECRET_ACCESS_KEY=your_r2_secret_access_key

# R2 Bucket 名称
KUN_VISUAL_NOVEL_S3_STORAGE_BUCKET_NAME=nwpushare-assets

# R2 Endpoint（格式固定，替换 <account_id>）
# 格式: https://<account_id>.r2.cloudflarestorage.com
KUN_VISUAL_NOVEL_S3_STORAGE_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com

# R2 Region（R2 使用 auto 即可）
KUN_VISUAL_NOVEL_S3_STORAGE_REGION=auto

# 公开访问 URL（需要配置自定义域名或使用 R2.dev 子域名）
# 选项 1: 使用 R2.dev 子域名（免费，有限制）
#   - 在 Bucket 设置中启用 "Public access" → "Allow Access"
#   - 格式: https://<bucket_name>.<account_id>.r2.dev
# NEXT_PUBLIC_KUN_VISUAL_NOVEL_S3_STORAGE_URL=https://nwpushare-assets.your_account_id.r2.dev

# 选项 2: 使用自定义域名（推荐，配合 Cloudflare CDN）
#   - 在 Bucket 设置中添加自定义域名
#   - 格式: https://cdn.yourdomain.com
# NEXT_PUBLIC_KUN_VISUAL_NOVEL_S3_STORAGE_URL=https://cdn.yourdomain.com

# 开发环境可以先用 R2.dev 子域名测试
NEXT_PUBLIC_KUN_VISUAL_NOVEL_S3_STORAGE_URL=https://nwpushare-assets.your_account_id.r2.dev
KUN_VISUAL_NOVEL_IMAGE_BED_URL=https://nwpushare-assets.your_account_id.r2.dev

# ================================
# R2 免费额度（2024年）
# ================================
# - 存储空间: 10GB/月
# - 写入请求: 100万次/月
# - 读取请求: 1000万次/月
# - 出站流量: 完全免费（这是 R2 的最大优势）
# 
# 对于头像存储来说完全够用！
